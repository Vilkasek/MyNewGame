// Include tile for build map
#include "Tile.hpp"

// Map struct
struct Tilemap
{
    // Tiles will be contained in two dimensional array
    Tile tiles[] = { 0 };

    // Initialize map
    void init() {}

    // Rendering tilemap
    void render() {}
}